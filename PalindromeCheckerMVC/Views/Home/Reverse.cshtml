@model PalindromeCheckerMVC.Models.Palindrome;

<div class="container px-4 py-5 mt-5">
    <div class="row gy-2 h-100 row-cols-1 row-cols-md-2">
        <div class="col-12 col-lg-6 border-end border-2">
            <h1 class="display-5 fw-bold lh-1 mb-3">
                Plaindrome Check/(String Reversal)
            </h1>
            <h4 class="lead pb-2">
                Here we reverse a string and test it is a palindrome. We use C# with MVC pattern
                A program that checks if the phrase entered is a palindrome. Display the result <strong style="color:green">Green</strong>.
            </h4>
            <h5>
                *Palinfrome is a word, phrase, or sequence that reads the same backward as forward.* e.g., <span style="font-style:italic">Tacocat</span>.
            </h5>
            <h2 class="mt-5 border-bottom border-2 ">
                Instructions
            </h2>
            <p>Enter a word or sentence. Press the button and the input will be evaluated on whether it is a palindrome or not. The resulting reversal of the string will also be appear. </p>
        </div>
        <div class="col-12 col-lg-6">
            <div>
                <h5>Enter a string</h5>

                @* Form goes here *@

                <form class="row gy-2 gx-3" asp-controller="Home" asp-action="Reverse" method="post">
                    <div class="col">
                        <input asp-for="InputWord" class="form-control"
                               placeholder="* type a word or phrase" value="@Model.InputWord"
                               aria-label="User String">
                    </div>
                    <div class="col-12 mt-2 d-flex align-items-center justify-content-end">
                        <button id="btnSubmit" class="btn btn-dark btn-lg" type="submit">
                            Palindrome Test
                        </button>
                    </div>
                </form>


                <h5 class="mt-5 border-bottom border-2">Reversed Result</h5>
                <div class="col">
                    <p class="form-control">@Model.RevWord</p>
                </div>


                <h2 class="mt-5 border-bottom border-2">Palindrome Result</h2>
                @if (Model.IsPalindrome)
                {
                    <div class="table-responsive">
                        <p class="bg-success" id="results">
                            @Model.Message
                        </p>
                    </div>
                }
                else
                {
                    <div class="table-responsive">
                        <p class="bg-danger" id="results">
                            @Model.Message
                        </p>
                    </div>
                }
            </div>
        </div>
    </div>
</div>